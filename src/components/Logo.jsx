import React from 'react';
import MainLogo from '../Images/Logo.webp';

const Logo = () => {
    return (
        <div>
            <img src={MainLogo} className='w-32' />
        </div>
    );
};

export default Logo;
